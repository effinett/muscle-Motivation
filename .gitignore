# Secrets — never commit keys (USDA, Stripe, Supabase service keys live in Vercel env)
.env
.env.*

# Dependencies — restored by `npm install`; Vercel installs its own copy
node_modules/

# OS noise
.DS_Store
